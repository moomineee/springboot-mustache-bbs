<!--- form 태그 --->
<!---
form으로 시작한다.
action : http request가 어디로 갈지
method : post가 없으면 기본값은 get
input name = "title" -> title을 서버에서 받아주어야 한다.
input name = "contetnt" -> content를 서버에서 받아주어야 한다.
type = subit -> 이 속성이 들어있는 버튼 등을 누르면 데이터(title, content)를 전송한다.
--->
{{>layouts/header}}
<form action="/articles/posts" method="post">
    <input type="text" name="title">
    <input type="text" name="content">
    <button type="submit" class="btn btn-primary">Submit</button>
    <a href="/articles">back</a>
</form>
{{>layouts/footer}}